#配置中心
spring:
  application:
    name: config-server
  cloud:
    config:
       server:
         git:
           uri: https://github.com/mujourney/wangxz-blog/
           username: XXXX
           password: XXXX
           search-paths: config

server:
  port: 2222

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:1111/eureka/
  instance:
    prefer-ip-address: true #注册时使用ip地址


